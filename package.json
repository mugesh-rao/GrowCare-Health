{
  "name": "growcare-desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:desktop": "node scripts/prepare-tauri-resources.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:frontend": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "test:clinical": "npm --prefix server run test:clinical"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
